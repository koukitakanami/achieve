<div class="container">
  <div class="wrapper col-md-8 col-md-offset-2 col-sm-10">
    <p><%= notice %></p>
    <h1>■ブログ一覧</h1>
      <%= link_to "ブログを作成する", new_blog_path, class: 'btn btn-default' %>
    <% @blogs.each do |blog| %>
    <div class="row">
      <!--p>投稿者：<!--%= blog.user.name %></p-->
        <table>
          <tr>
            <td><%= profile_img(blog.user) if profile_img(blog.user) %></td>
            <td align = "left"><h5><p>投稿者：</p><%= blog.user.name %></h5></td>
          </tr>
        </table>
      <h4><strong><%= blog.title %></strong></h4>
      <p><%= blog.content %></p>
      
      <%= link_to "編集", edit_blog_path(blog.id), class: 'btn btn-default btn-sm btn-success' %>
      <%= link_to "削除", blog_path(blog.id), method: :delete, data: { confirm: '本当に削除していいですか？'}, class: 'btn btn-default btn-sm btn btn-danger' %>
    </div>
    <% end %>
    
    <br>
    <!--%= link_to "ブログを作成する", new_blog_path %-->
    <!--%= link_to "トップページにもどる", toppage_index_path %-->
  </div>
</div>


<!--%= link_to "お問い合わせ", new_contact_path %>